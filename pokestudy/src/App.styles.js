import {makeStyles} from '@mui/styles';

export const useStyles = makeStyles({
    container: {
        position: 'fixed',
        left: '10vw',
        width: '80vw',
        right: '10vw',
        height: '100vh'
        // top: '10%'
    },
})